You are a senior Python backend architect with deep production experience in FastAPI, PostgreSQL, async systems, MQTT, GPS-based routing, security, testing, and scalable API design. Build the complete backend for my project “Edge-AI Based Adaptive Traffic Management System with Intelligent Ambulance Priority.” Create a new folder named `server` and place the entire backend inside it. Use Python 3.12+, FastAPI, PostgreSQL, JWT authentication, HTTPX, pytest, and structured logging. Use `uv` for the complete Python workflow, not pip. Create the virtual environment using `uv venv`, configure `pyproject.toml`, install all dependencies using `uv add`, and make the project runnable using `uv run`. Do not create frontend, Raspberry Pi inference code, YOLO training code, or mobile UI. The backend must be production-structured, fully modular, type-safe, async wherever appropriate, easy to test, and designed so real services can replace mocked integrations without rewriting business logic. Do not place everything inside `main.py`; use proper modules for configuration, database, models, schemas, repositories, services, API routes, integrations, utilities, middleware, and tests.

The architecture is a single centralized FastAPI backend running on AWS while every Raspberry Pi independently controls one four-way traffic junction. Each junction has four independently controlled approaches: NORTH, SOUTH, EAST, and WEST; do not combine them into North-South or East-West phases. The Raspberry Pi performs camera capture, BMD-45 pretrained YOLO inference, 14-to-5 vehicle-class mapping, ByteTrack tracking, vehicle counting, queue estimation, density estimation, and adaptive traffic decisions locally. The backend must never require continuous camera streams or perform normal traffic-light decision making. It receives compact telemetry from each Pi and manages centralized coordination. Create entities and APIs for users, ambulance drivers, ambulances, junctions, four junction approaches, Raspberry Pi devices, telemetry, emergency sessions, GPS locations, emergency commands, device heartbeats, and audit logs. Use UUID primary keys, timezone-aware UTC timestamps, foreign keys, indexes, constraints, enums, clean database relationships, pagination, filtering, and migrations. Keep repository/database logic separate from service/business logic and API controllers. Include `/health`, `/ready`, versioned routes under `/api/v1`, consistent response/error schemas, centralized exception handling, request IDs, CORS configuration, and environment-based configuration using `.env` and `.env.example`.

Implement complete authentication and ambulance management. Support driver registration, login, refresh token, current-user endpoint, password hashing, JWT access/refresh tokens, ambulance registration, ambulance assignment to a driver, and role-based permissions such as ADMIN and DRIVER. During development use mock secrets defined through environment variables, but never hardcode secrets directly inside application code. The ambulance driver app will always display an Emergency button. When the driver presses Start Emergency, the backend must create an active emergency session associated with that authenticated driver and ambulance. After activation, the mobile app continuously sends GPS samples containing latitude, longitude, accuracy, speed, heading/bearing, timestamp, and emergency-session ID. Validate all GPS input and reject updates for unauthorized or inactive sessions. Persist useful GPS history without creating unnecessary duplication. Implement Start Emergency, GPS Update, Current Emergency, and Stop Emergency APIs. Prevent multiple conflicting active emergency sessions for the same ambulance. Add session states such as CREATED, ACTIVE, APPROACHING_JUNCTION, PRIORITY_REQUESTED, CROSSING, COMPLETED, CANCELLED, and TIMED_OUT. Add configurable inactivity and emergency timeouts so a forgotten emergency session cannot remain active forever.

Implement the complete junction-selection and ambulance-priority business logic. Each junction must store latitude, longitude, detection radius, and exactly four approach definitions. Every approach should support direction, optional heading range, entry/geofence coordinates or configurable approach metadata. Whenever an active ambulance GPS update arrives, calculate nearby registered junctions using a correct geographic distance method such as Haversine or an equivalent service abstraction. Select only junctions within a configurable radius and determine whether the ambulance is actually approaching instead of merely being geographically close. Use the ambulance’s current position, previous positions, movement vector, speed, bearing/heading, distance trend, and configured approach geometry to determine NORTH, SOUTH, EAST, or WEST. Prefer a junction when distance is decreasing and heading is consistent with entering it. Prevent false priority after the ambulance has crossed the junction or is moving away. When a valid approaching junction and approach are identified, create an idempotent emergency-priority command addressed only to that junction’s Raspberry Pi. Include emergency ID, junction ID, approach, command type, timestamp, expiry/TTL, and correlation ID. After detecting that the ambulance has crossed and is leaving the junction, generate a RELEASE_PRIORITY command and allow the session to continue toward the next junction if needed. Design this logic as isolated services so the geographic/routing algorithm can later be improved without changing APIs.

Create integration abstractions for MQTT, push notifications, and external services even though real credentials will be provided later. Define an MQTT client interface with publish/subscribe behavior and a mock implementation that logs and stores outgoing messages. Structure topics similar to `junction/{junction_id}/telemetry`, `junction/{junction_id}/heartbeat`, `junction/{junction_id}/command`, and `junction/{junction_id}/emergency`. Emergency commands must support acknowledgements, idempotency, retry metadata, expiry, and status values such as PENDING, SENT, ACKNOWLEDGED, FAILED, EXPIRED, and RELEASED. Implement REST endpoints as a fallback so a Raspberry Pi can poll or acknowledge pending commands when MQTT is unavailable. Create a notification service abstraction for OneSignal with a mock implementation and placeholder configuration variables such as `ONESIGNAL_APP_ID` and `ONESIGNAL_API_KEY`; do not call real OneSignal services yet. Do the same for MQTT credentials and broker URL. Implement device registration, device authentication/API-key strategy, heartbeat handling, online/offline status calculation, telemetry ingestion, latest junction state, historical telemetry querying, emergency history, and admin endpoints required by a future React dashboard. Do not use mock fake business logic where real local logic can already be implemented; only external integrations should be mocked.

Finish the backend as a complete runnable project, not a scaffold. Create a clear folder structure under `server/app` with modules such as `core`, `db`, `models`, `schemas`, `repositories`, `services`, `api`, `integrations`, and `utils`, plus `tests`. Add Alembic migrations, database initialization, seed/demo data for at least one junction with four independent approaches, one Raspberry Pi device, one driver, and one ambulance. Provide mocked configuration values in `.env.example`, Docker support for PostgreSQL if useful, startup/shutdown lifecycle handling, structured logging, validation, indexes, rate-limit-ready middleware structure, and meaningful API documentation through FastAPI/OpenAPI. Add unit tests for Haversine distance, approach-direction detection, junction selection, emergency lifecycle, authorization, idempotent emergency command creation, priority release, timeout behavior, telemetry ingestion, and mocked MQTT publishing; add API integration tests for critical routes. Run formatting, linting, type checking, tests, and application startup checks and fix all errors before stopping. Use Ruff and mypy or equivalent strict tooling. Also create a concise `README.md` containing exact commands: `cd server`, `uv venv`, dependency setup, environment setup, database migration, seed command, test command, and `uv run uvicorn app.main:app --reload`. At the end, print the final folder tree, list implemented APIs, list environment variables still requiring real values tomorrow, and mention any assumptions you made. Do not leave TODO placeholders for core backend functionality; external credentials may remain mocked, but the backend itself must work end-to-end today.